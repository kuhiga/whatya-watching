/* eslint-disable no-unused-vars */
export enum ColumnType {
    TO_WATCH = 'To Watch',
    IN_PROGRESS = 'In Progress',
    PARKED = 'Parked',
    COMPLETED = 'Completed',
}

export enum ItemType {
    MOVIE = 'Movie',
    SHOW = 'Show'
}